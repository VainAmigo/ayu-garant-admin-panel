import 'package:flutter/material.dart';

class UserReportView extends StatelessWidget {
  const UserReportView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('User Report')],);
  }
}