import 'package:flutter/material.dart';

class EmergencyCommissionerView extends StatelessWidget {
  const EmergencyCommissionerView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Emergency Commissioner')));
  }
}